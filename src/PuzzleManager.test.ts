import "./PuzzleManager";

describe("PuzzleManager", () => {
    // Not many easy tests for this without resorting to E2E tests
    // TODO: Unless, I make all PuzzleManager dependencies swappable (dependency-injection)
    it.todo("puzzle manager is too tightly coupled right now");
});
